{
  "name": "dynamic-redirector",
  "version": "1.0.0",
  "description": "Cloud Run service for dynamic URL redirection",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=dynamicRedirect --port=8080",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=16"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "@google-cloud/functions-framework": "^3.0.0",
    "express": "^4.18.2"  
  }
}
